@page
@model anuR.Views.Admin.Index;

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>@for (int i = 0; i < @Model.Sites.Count; i++)
     {
            <div>
                <a href="@Model.Sites[i].City">@Model.Sites[i].Id</a>
            </div>             
     }
</div>
